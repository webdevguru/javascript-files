function greet(name) {
  return "Hello, " + name + "!";
}

console.log(greet("Alice")); // Outputs: "Hello, Alice!"


const greet = name => {
  return "Hello, " + name + "!";
};

console.log(greet("Alice")); // Outputs: "Hello, Alice!"
